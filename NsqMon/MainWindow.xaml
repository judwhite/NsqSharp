<views:WindowViewBase
    x:Class="NsqMon.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:views="clr-namespace:NsqMon.Views.CDTag.Views"
    xmlns:common="clr-namespace:NsqMon.Common.Wpf"
    Title="nsqmon" 
    Height="700" 
    Width="1024"
    MinHeight="400"
    MinWidth="600"
    Background="#005A90"
    WindowStartupLocation="CenterScreen"
    ShowInTaskbar="True"    
>
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="24" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200" />
            </Grid.ColumnDefinitions>
            <ComboBox ItemsSource="{Binding Clusters, UpdateSourceTrigger=PropertyChanged}" DisplayMemberPath="DisplayName" SelectedItem="{Binding SelectedCluster}" />
        </Grid>

        <common:ErrorContainer x:Name="ErrorItems" Grid.Row="0" />

    </Grid>

</views:WindowViewBase>
